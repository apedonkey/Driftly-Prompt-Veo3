# Production Environment Variables for Railway
# Only system configuration needed - users provide their own API keys

# Flask Configuration (Required)
FLASK_ENV=production
FLASK_SECRET_KEY=generate-a-very-long-random-string-here

# Optional Security
USE_AUTH=false  # Set to true if you want password protection
ADMIN_PASSWORD_HASH=  # Only needed if USE_AUTH=true